# 副摄像头B方案（哨兵背后长眼睛）

## 1. 背景与目标

已有主摄像头 A，希望在哨兵背后新增一个 usb 摄像头，作为副摄像头 B，看见背后的敌人。

- 如果 A 的视野没有可击打装甲板，但 B 的视野中有，则需哨兵回头去打敌人
- 如果 A 和 B 的视野中同时出现可击打装甲板，优先采取 A 视野中的目标
- B 只负责简单的看到，目的只是让哨兵转身

## 2. 现状与约束（代码事实）

- `rm_detector` **硬编码**订阅 `/image_raw`（`src/rm_detector/src/node.cpp:112`），单路图像输入；同时订阅 `/camera_info` 做 PnP 解算
- `usb_camera` 包现成（package 名 `usb_camera`，topic 为相对名 `image_raw`），把 B 放进独立 namespace（如 `/b_camera`）即可与 A 共存，topic 为 `/b_camera/image_raw`
- **关键坑**：A/B 切换必须**同步切换 image 和 camera_info**，否则 PnP 用错内参，坐标全错
- B 是独立相机，需要第二份标定文件（camera_info.yaml）与参数文件（camera_params.yaml）

## 3. 候选架构

### 方案 1：新增 selector 节点（推荐）

```
A 相机 ──► /image_raw + /camera_info ──┐
                                       ├──► selector ──► /image_raw + /camera_info ──► rm_detector（零改动）
B 相机 ──► /b_camera/image_raw + /b_camera/camera_info ──┘
```

- selector 按 A 优先策略把 A/B 两路 image + camera_info 二选一转发
- detector / tracker / 自瞄链路**零改动**，风险最小
- 代价：新增一个节点的开发量

### 方案 2：detector 双订阅 + 内部切换

- detector 同时订阅 A/B 两路，内部选择
- 侵入现有检测链路，调试风险大，不推荐

## 4. 切换策略（参数待定）

- **A 优先**：只要 A 有可击打装甲板，永远用 A
- **切 B 条件**：A 连续 N 帧/秒无装甲板 + B 有检测（N 待定，建议 0.5~1s）
- **切回 A 条件**：切 B 后 A 重新检测到装甲板并持续 M 帧（回滞防抖动，M 待定）
- **"可击打"判定**：B 侧只做检测级判定（检出装甲板即算）；A 侧沿用现有 tracker 判定（检测 + can_fire）

## 5. 转身机制（待定）

| 路线 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| 路线 1：云台转身 | B 检出目标 → tracker 输出大角度 yaw 增量 → 云台转过去 | 走现有 `gimbal_control` 链路，无需导航方参与 | 云台转角范围受限（能否 360° 待确认）；转身时 B 视野随之变化 |
| 路线 2：底盘转身 | 视觉方给导航方发"后方发现敌人"信号 → 导航方控制底盘转身 | 视野稳定 | 需要导航方配合；`enemy_datas` 需增加字段，通信接口计划同步升级 |

**与通信接口计划的关系**：若选路线 2，需在 `enemy_datas` 中增加"后方发现"信号，并回改《通信接口计划》的消息定义与沟通确认事项。

## 6. 硬件与标定待定项

- B 相机型号、安装位置（底盘固定 vs 云台跟随）、朝向与俯仰角
- B 的 camera_info.yaml 标定
- B 的 namespace / 话题名约定（建议 `/b_camera/image_raw`）

## 7. 待讨论清单

- [ ] 方案选择：selector 节点（方案 1）还是其他
- [ ] 转身执行方：云台（路线 1）还是底盘（路线 2）
- [ ] B 安装位置与朝向（影响转身后 B 视野是否仍然有效）
- [ ] 切换参数 N / M（切 B 与切回 A 的连续帧数）
- [ ] "可击打"判定标准确认（B 检测级、A 检测 + can_fire）
- [ ] B 相机的标定与参数文件由谁维护

## 8. 分阶段里程碑

1. **B 单独跑通**：B 相机出图 + 独立跑通检测（类似现有 `usb_nav` 链路），确认 B 视野内检测效果
2. **selector + A 优先策略**：实现 A/B 二选一转发与切换策略，detector 链路零改动验证
3. **转身联调**：依赖第 7 节"转身执行方"结论，联调转身动作与切回 A 的条件
