# DT46 视觉项目 × 导航项目 ROS2 通信接口计划（v2.0）

## 1. 背景与目标

视觉自瞄项目与导航项目在同一台机器人上运行，均基于 ROS 2 Humble，通过话题通信。

视觉方需要向导航方提供五类数据：

1. **云台增量 yaw**：识别到装甲板时，云台需要转动的 yaw 增量（度）
2. **敌人车辆中心点**：EKF 滤波后的世界惯性系坐标（米）
3. **车体朝向 enemy_yaw**：敌人车体的世界系朝向（弧度，EKF 滤波值）
4. **兵种编号 armor_id**：detector 分类结果（0-11，见 README 表）
5. **跟踪状态 tracked**：当前是否有有效目标

## 2. 总体链路

```
相机 → rm_detector → rm_tracker ── /tracker/gimbal_control ──► rm_serial ──► 电控（MCU，已有链路）
                                   ├─ /tracker/enemy_datas ───► rm_tf_broadcaster ── tf: map→enemy
                                   │                                                        └─ /tracker/enemy_pose
                                   └─ /tracker/gimbal_control ─► 导航项目（可选订阅，多订阅不影响 rm_serial）
```

## 3. 接口总表

| 话题 | 类型 | 现有消费者 | 导航方可选订阅 | 频率 | QoS | 状态 |
|---|---|---|---|---|---|---|
| `/tracker/gimbal_control` | `rm_interfaces/msg/GimbalControl` | rm_serial（下发 MCU/电控） | 是（yaw 与 enemy_datas 同值，可不订） | 随检测帧率 | BEST_EFFORT | 已有，不动 |
| `/tracker/enemy_datas` | `rm_interfaces/msg/EnemyCenter` | rm_tf_broadcaster | 是 | 随检测帧率 | BEST_EFFORT | 新增（v2.0 字段见第 4 节） |
| `/tracker/enemy_pose` | `geometry_msgs/msg/PoseStamped` | — | 是（map 系位姿，与 tf 同值） | 随检测帧率 | DEFAULT | 新增（v2.1，rm_tf_broadcaster 发布） |

## 4. 新消息定义

文件：`src/rm_interfaces/msg/EnemyCenter.msg`

```msg
std_msgs/Header header   # 时间戳 = 图像曝光时刻；frame_id = tracking_frame
float32 yaw              # 云台需要转动的 yaw 增量（度），与 /tracker/gimbal_control 的 yaw 同值
float32 x                # 敌人车辆中心 x，世界惯性系，米（EKF 状态[0]）
float32 y                # 敌人车辆中心 y，世界惯性系，米（EKF 状态[2]）
float32 z                # 敌人车辆中心 z，世界惯性系，米（EKF 状态[4]）
float32 enemy_yaw        # 敌人车体朝向，世界系，弧度（EKF 状态[6]）
int32   armor_id         # 兵种编号 0-11（B1-B7/R1-R7，表见 README）
bool    tracked          # 有效跟踪标志（非 LOST 为 true）
```

**丢失目标（LOST）约定**：`tracked=false` 为主判据；数值全 0；`armor_id=-1`（避免与合法编号 0 冲突）。

## 5. 视觉方（本项目）工作清单

1. **新建分支**：基于当前开发分支 `2026` 新建功能分支 `nav_interface`，本次接口改动全部在该分支完成，与主线开发互不影响；联调验证通过后再合回 `2026`
2. **步骤概览**：本次视觉方改动只涉及 3 个文件、新增 1 个话题，不改动任何现有链路：
   - `rm_interfaces`：新增 `msg/EnemyCenter.msg` 并注册（下文第 3、4 步）
   - `rm_tracker`：在 `node.py` 新增 `/tracker/enemy_datas` 发布（下文第 5 步）
   - 自测与验收（下文第 7、8 步）
3. **新建消息**：按第 4 节创建 `src/rm_interfaces/msg/EnemyCenter.msg`
4. **注册消息**：`src/rm_interfaces/CMakeLists.txt` 的 `rosidl_generate_interfaces` 列表中加入 `"msg/EnemyCenter.msg"`
5. **修改 `src/rm_tracker/rm_tracker/node.py`**：
   - import `EnemyCenter`
   - 在 publisher 创建区（`/tracker/gimbal_control` 附近）新增：
     `self.pub_enemy_datas = self.create_publisher(EnemyCenter, '/tracker/enemy_datas', qos_profile_sensor_data)`
   - 在处理线程内、`track()` 与 snapshot 提取之后组包发布：
     - `yaw = gimbal_control[0]`（现成的增量角，度，与 gimbal_control 话题同值）
     - `x / y / z = snapshot['target_state'][0 / 2 / 4]`（世界惯性系，米）
     - `enemy_yaw = snapshot['target_state'][6]`（车体朝向，弧度）
     - `armor_id = snapshot['target'].id`（当前锁定板分类编号 0-11）
     - `tracked = snapshot['tracker_state'] != LOST`
     - LOST 时：`tracked=false`、数值全 0、`armor_id=-1`
     - `header.stamp = ros_clock`（图像原始时间戳），`frame_id = 'tracking_frame'`
6. **不改动** `/tracker/gimbal_control`
7. **编译自测**：
   ```bash
   colcon build --symlink-install
   source install/setup.bash
   ros2 interface show rm_interfaces/msg/EnemyCenter
   ros2 topic info /tracker/enemy_datas -v      # 核对 QoS 为 BEST_EFFORT
   ros2 topic echo /tracker/enemy_datas
   ```
8. **验收标准**：跟踪目标时 yaw / x / y / z / enemy_yaw 数值连续合理，armor_id 与目标兵种一致；丢失目标后 `tracked=false`、数值全 0、`armor_id=-1`
9. **新建 `rm_tf_broadcaster` 包**（v2.1 新增，不动 rm_tracker）：
   - 位置：`src/rm_tf_broadcaster/`，ament_python 包
   - 节点 `enemy_tf_node`：
     - 订阅 `/tracker/enemy_datas`（`EnemyCenter`，QoS BEST_EFFORT）
     - `tracked=true` 时发布 tf `map→enemy`（`TransformStamped`）+ 话题 `/tracker/enemy_pose`（`PoseStamped`，map 系）
     - `tracked=false` 时不发布（enemy 帧从 tf 树消失）
   - 改动 `src/rm_DT46/launch/sentry_nav.launch.py`，追加 Node 启动 `rm_tf_broadcaster`

## 6. 接收方（导航方）工作清单

1. 将 `rm_interfaces` 包源码放入自己的 workspace 并编译（消息类型必须完全同名）
2. 订阅 `/tracker/enemy_datas`，**QoS 必须用 BEST_EFFORT**（发布端是 BEST_EFFORT，RELIABLE 收不到）
3. 如需 pitch / can_fire，可额外订阅 `/tracker/gimbal_control`（QoS 同为 BEST_EFFORT）；只关心 yaw 时不必订，`enemy_datas` 已含同值
4. 若导航方有反馈 / 指令需要发给视觉方，提供话题名表

## 7. 双方必须沟通确认的事项

- [ ] 7.1 接口包归属与同步：`rm_interfaces` 由谁维护？改 msg 必须双方同时升级编译（建议独立 git 仓库，双方 clone）
- [ ] 7.2 话题名：`/tracker/enemy_datas` 双方认可
- [ ] 7.3 QoS：BEST_EFFORT + depth 双方一致
- [ ] 7.4 单位：yaw 为度（增量角）；x / y / z 为米；enemy_yaw 为弧度；armor_id 编号 0-11（表见 README）
- [ ] 7.5 坐标系定义：x / y / z 为世界惯性系（原点 = IMU/云台），轴向需场地实测确认（代码 `tracker.py` 的 `cam_to_world` 由相机系旋转而来，文档与实测对照）
- [ ] 7.6 yaw 语义：**云台增量（度），非世界系绝对角**；导航方若需要绝对世界角，需另行合成（IMU yaw + 相对角）
- [ ] 7.7 enemy_yaw 语义：敌人车体朝向（EKF 滤波值，弧度，-π ~ π），**0 方向和正方向需场地联调确认**
- [ ] 7.8 LOST 语义：`tracked=false` 为主判据，数值全 0，`armor_id=-1`
- [ ] 7.9 gimbal_control 语义：yaw / pitch 为相对当前云台的增量（度）+ can_fire
- [ ] 7.10 运行环境：同机、`ROS_DOMAIN_ID` 一致
- [ ] 7.11 联调安排：场地、时间表；双方各定一名对接人

## 8. 联调与验收流程

1. 视觉方单机自测（第 5.7 节）
2. 双项目同机启动，导航方 `ros2 topic echo /tracker/enemy_datas` 验证收到
3. 场地实测：静态目标 → 运动目标，核对数值符号与量级（正前方目标 x / y / z 大概应为多少、enemy_yaw 的 0 方向与正方向、armor_id 与目标兵种是否一致）
4. 问题记录 → 接口变更统一由视觉方修改文档与消息定义，双方同步升级

## 9. 版本记录

| 版本 | 变更 |
|---|---|
| v1.0 | 初稿：`yaw + x/y/z` 四字段；LOST 约定为全 0 |
| v2.0 | 新增 `enemy_yaw` / `armor_id` / `tracked` 三字段；LOST 约定升级为 `tracked=false` + 全 0 + `armor_id=-1` |
| v2.1 | 新增 `rm_tf_broadcaster` 包：订阅 `/tracker/enemy_datas`，用 tf2_ros 发布 tf `camera→enemy` + 话题 `/tracker/enemy_pose`；改动 `sentry_nav.launch.py` |
